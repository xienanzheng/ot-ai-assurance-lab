# Homepage future work and Git reconciliation

Removed Track II and US–China content from the homepage. Added Future work navigation, planned OpenJev decision evaluation and a latency roadmap. The playful teaser has pointer evasion, keyboard activation, a stationary equivalent, and reduced-motion support.

Resolved the paused rebase onto 042d565 by retaining both new inference/retrieval settings and upstream hosted non-actuation enforcement plus accurate cloud-status copy. The earlier inference work is now commit 3b5aeb3. No force push is required.

Validation: homepage and simulator builds; 91 targeted Python checks; 15 JavaScript checks, including hosted actuation denial; browser verification at 320, 390, 768, 820, 1024, 1512 and 1920 pixels without document overflow. Pointer evasion, keyboard reveal and reduced-motion stability verified. Private demo manual and .env.local are outside the publication contents.

Only the homepage is deployed in this update; the new local inference implementation is published as source, not deployed to the hosted simulator.
